<div style="display: flex; min-height: 100vh; background-color: #121212; color: white; width: 100%;">

    <div style="width: 280px; min-width: 280px; background-color: #000; border-right: 1px solid #333; display: flex; flex-direction: column;">
        <div class="top-row ps-3 navbar navbar-dark bg-dark border-bottom border-secondary" style="height: 3.5rem; display: flex; align-items: center;">
            <div class="container-fluid">
                <a class="navbar-brand fw-bold text-primary" href="">
                    <i class="bi bi-cpu-fill me-2"></i>Compilador C+
                </a>
            </div>
        </div>

        <div class="nav-scrollable" style="flex: 1; overflow-y: auto;">
            <nav class="flex-column mt-2">
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link custom-nav-link" href="" Match="NavLinkMatch.All">
                        <i class="bi bi-house-door-fill fs-5 me-3"></i>
                        <span>Inicio</span>
                    </NavLink>
                </div>

                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link custom-nav-link" href="compilador">
                        <i class="bi bi-terminal-fill fs-5 me-3 text-info"></i>
                        <span>Compilador C+</span>
                    </NavLink>
                </div>

                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link custom-nav-link" href="consulta">
                        <i class="bi bi-database-fill-gear fs-5 me-3 text-warning"></i>
                        <span>Historial (CRUD)</span>
                    </NavLink>
                </div>
            </nav>
        </div>
    </div>

    <main style="flex: 1; height: 100vh; overflow-y: auto; padding: 20px;">
        <Router AppAssembly="typeof(Program).Assembly">
            <Found Context="routeData">
                <RouteView RouteData="routeData" />
                <FocusOnNavigate RouteData="routeData" Selector="h1" />
            </Found>
            <NotFound>
                <div class="p-4 text-center">
                    <h3 class="text-danger">404</h3>
                    <p>Página no encontrada.</p>
                </div>
            </NotFound>
        </Router>
    </main>

</div>

<style>
    .custom-nav-link {
        color: #adb5bd !important;
        transition: all 0.3s ease;
        padding: 0.8rem 1rem !important;
        text-decoration: none;
        display: flex;
        align-items: center;
        border-radius: 8px;
    }

        .custom-nav-link:hover {
            background-color: #1a1a1a !important;
            color: #0d6efd !important;
            transform: translateX(5px);
        }

        .custom-nav-link.active {
            background: linear-gradient(90deg, #0d6efd33 0%, #000000 100%) !important;
            color: #0d6efd !important;
            border-left: 4px solid #0d6efd;
        }

        .custom-nav-link i {
            width: 30px;
            text-align: center;
        }

    /* Reset de estilos para asegurar que ocupe toda la pantalla */
    body, html {
        margin: 0;
        padding: 0;
        height: 100%;
        overflow: hidden;
    }
</style>